name = "jingangchong-app"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages 配置
# 通过 Cloudflare 网页面板连接 GitHub 时，以下配置在面板中设置
# 通过 wrangler CLI 部署时，使用以下配置
[build]
command = "npm run build && npm run build:pages"
publish = ".vercel/output/static"